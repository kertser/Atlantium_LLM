# Ongoing Tasks / Errors:

* Visualize the document processing with progress bar - over the "processing" button (processing batches)
* Add prompt template for Interactive troubleshooting mode (analyze step-by-step)
* Add option for "chat with live operator" (A button with contacts or whatsapp)
* Update documentation

* Check calculator for RZ-UHP
* Attach Image on the frontend is not not loading correctly without reset, after images been founded in query
* Try to filter the document reference images from the search results images
* Fix system message "localhost:9000 wants to delete" etc...
* Add frontend check for file format XXXXX-Filename.ext
* During the image store the images shall be saved rotated as they appear in document.
* Resolve reloaded CLIP and FAISS on every upload. It is done at the server start and relevant values shall be passed.
* Some stored images are very much similar to each other, need to remove duplicates ??
* Check for Excel errors (images)
* Update the requirements.txt for new model usage, depending on system


# TBDs for development:
* Add complicated reasoning (SMoA) to the model (TBD)
* Switch to MongoDB instead of metadata in files (TBD)
* Android mobile app development (TBD)
* Offline and fine-tuned LLMs (TBD)
* Multimodal Input - Connect audio model (TBD)
* Adaptive retrieval thresholds (TBD)

pip3 install -U xformers --index-url https://download.pytorch.org/whl/cu124
pip install ninja

This is for windows:
download from https://github.com/bdashore3/flash-attention/releases/download/v2.7.1.post1/flash_attn-2.7.1.post1+cu124torch2.5.1cxx11abiFALSE-cp312-cp312-win_amd64.whl
and pip install

For Linux is probably just: pip install flash-attn (probably with GPU only)