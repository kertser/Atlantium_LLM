# General Tasks:
* Test docker-compose with updates
* Add prompt template for Interactive troubleshooting mode (analyze step by step)
* Add option for "chat with live operator" (A button with contacts or whatsapp)
* Calculator API - connect as agent

# Urgent Tasks / Errors:
* Play with similarity scores to have more precise answers.
* Continue playing with prompts, top_K and chunking to reduce hallucinations. (May be achieved with more data)
* Need to treat headers and footers of pdf/doc files form chinking/indexing - it is a garbage data. Similarly - prefaces, tables of content
  One good idea is to keep this data with special metadata entry: 'block_type': position,  # 'header', 'footer', 'toc', 'content'
  Then adding flags in the query processing to control whether to include this content and weight different content types differently in relevance scoring
  It will provide more data in format_response function
* Visualize the document processing with progress bar - over the "processing" button
* Search the image and faiss metadata to retrieve the context from the documents, containing the images, similar to the attached.
* Update methods of content treatment and chat history (including prompt templates). If the query is irrelevant to the history, do not take the history into account
* Combine functionality of prompt_loader.py and prompts.py into a single file -> prompt_management.py
  image_store and image_utils into a single library -> image_utils.py, keeping the functionality.


# TBDs for development:
* Switch to MongoDB instead of metadata in files
* Android mobile app development (TBD)
* Offline and fine-tuned LLMs (TBD)
* Multimodal Input - Connect audio model (TBD)

# Documentation:
- manual deploy without updates (docker and with no docker)
- manual deploy with updates (docker and no docker)
- update documentation
