# Ongoing Tasks / Errors:
------------------------------------------------------------------------------------------------------------
* Visualize the document processing with progress bar - over the "processing" button (processing batches)
May-be trigger endpoint with percentage?
* Replace "\\" with "/" - add .as_posix() to the path to convert it to string object with "/"
(May be replace absolute paths with relative, for compatibility with Linux)
* Make ~= for strict requirements for cache. Add USER appuser into dockerfile
PROBLEM with processed_files - finds all the documents instead of only new ones

# TBDs for development:
------------------------------------------------------------------------------------------------------------
* Add complicated reasoning (SMoA) to the model (TBD)
* Switch to MongoDB instead of metadata in files (TBD)
* Offline and fine-tuned LLMs (TBD)
* Multimodal Input - Connect audio model (TBD)
